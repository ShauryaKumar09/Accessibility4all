# Accessibility4all — shared dependencies (one venv for the hub + all features)
# Install with:  pip install -r requirements.txt
# Supports macOS and Windows.
groq==1.4.0
numpy==2.4.6
pillow==12.2.0
PyAutoGUI==0.9.54
pynput==1.8.1
pyperclip==1.9.0
pytesseract==0.3.13
python-dotenv==1.2.2
pyttsx3==2.98
sounddevice==0.5.5
SpeechRecognition==3.16.1
# macOS-only: global ` key listener in voice_control
pyobjc-core==12.2; sys_platform == 'darwin'
pyobjc-framework-Quartz==12.2; sys_platform == 'darwin'
# Windows-only: focus Chrome window
pygetwindow==0.0.9; sys_platform == 'win32'
